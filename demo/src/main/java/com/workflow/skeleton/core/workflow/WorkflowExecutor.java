package com.workflow.skeleton.core.workflow;

import org.springframework.stereotype.Service;

@Service
public class WorkflowExecutor {
	public void submitWorkTasks(WorkflowDefine workflowDefine) {
		// TODO change workflow to task
		// TODO save to DB Task
		// TODO long taskId = dao.insertTask(attemptId, task.getParentId().orNull(), task.getTaskType().get());  // tasks table don't have unique index
		
		
	}
    
}
